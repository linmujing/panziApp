//index.js
//获取应用实例
const app = getApp()

Page({
  data: {
    indicatorDots: true,
    cindicatorColor: '#fff',
    autoplay: true,
    interval: 5000,
    duration: 1000,
  },
  onLoad: function () {
    
  },
  blur_search: function (e) {
    // this.setData({
    //   'tel': e.detail.value
    // })
  },
})
